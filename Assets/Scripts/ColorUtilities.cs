using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColorUtilities : MonoBehaviour {

	//IEnumerator ChangeColorGradually( Renderer rend, Color a, Color b, float speed) {
	//	float progress = 0;

	//	while (progress < 1) {
	//		rend.material.color = Color.Lerp(a, b, progress);
	//		progress += speed;
	//		yield return null;
	//	}
	//}
}
